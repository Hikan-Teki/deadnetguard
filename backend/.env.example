# Database - Replace with your actual credentials
DATABASE_URL="postgresql://user:password@localhost:5432/deadnetguard"

# Server
PORT=3001
NODE_ENV=development

# Security - Set in production
# EXTENSION_ID="your-chrome-extension-id"

# CORS (comma-separated origins) - optional, defaults are set in code
# CORS_ORIGINS="chrome-extension://your-extension-id,https://deadnetguard.com"
